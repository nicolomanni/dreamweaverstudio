services:
  - type: web
    name: dreamweaver-server
    env: node
    plan: starter
    buildCommand: npm ci --include=dev && npx nx build server
    startCommand: node dist/apps/server/main.js
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: STRIPE_SECRET
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: FIREBASE_PROJECT_ID
        sync: false
      - key: FIREBASE_CLIENT_EMAIL
        sync: false
      - key: FIREBASE_PRIVATE_KEY
        sync: false
  - type: static
    name: dreamweaver-client
    env: static
    buildCommand: npm ci --include=dev && npx nx build client
    staticPublishPath: dist/apps/client
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
